# Tables with more than one Eloquent model.
#
# Two models on one table disagree about it: one names columns the table has
# and the other names columns it does not, so the code on one works and the
# code on the other answers 500.
#
# Every table has one model, so this list is empty. A ratchet: it may not
# grow, and a table that gains a second model fails the test.
